---
description: Study & update project — reads every file in the repo, syncs its README, and prepares full context for your next request. Runs your "study-update-project" prompt directly (skips the menu)
---

Invoke the **prompt-shortcut** skill (use the Skill tool) and run its **study-update-project** prompt directly, without showing the menu.
