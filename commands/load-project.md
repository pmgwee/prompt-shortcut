---
description: Load full project context — reads every file in the repo, syncs its README, and prepares full context for your next request. Runs your "load-project" prompt directly (skips the menu)
---

Invoke the **prompt-shortcut** skill (use the Skill tool) and run its **load-project** prompt directly, without showing the menu.
