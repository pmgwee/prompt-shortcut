---
description: Package / install a skill — runs your "package" favorite prompt directly (skips the menu)
---

Invoke the **prompt-shortcut** skill (use the Skill tool) and run its **package** prompt directly, without showing the menu.
