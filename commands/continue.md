---
description: Continue working — runs your "continue" favorite prompt directly (skips the menu)
---

Invoke the **prompt-shortcut** skill (use the Skill tool) and run its **continue** prompt directly, without showing the menu.
